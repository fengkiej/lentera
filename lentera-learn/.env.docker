# API Base URL for Docker environment (connecting to backend container)
NODE_ENV=development
VITE_API_BASE_URL=http://backend:3000/api/v1

# CORS configuration - wildcard to allow any origin in Docker environment
CORS_ORIGIN=*

# Simple auth configuration
API_SECRET_KEY=lentera-dev-secret-key-change-in-production